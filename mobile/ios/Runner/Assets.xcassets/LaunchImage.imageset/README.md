# Travyon iOS Launch Image

The transparent paper-plane images are generated by `scripts/build-ios-launch-image.ps1`.
The orange background is set in `Runner/Base.lproj/LaunchScreen.storyboard` and
`Runner/Base.lproj/Main.storyboard` to match the Android launch screen.
